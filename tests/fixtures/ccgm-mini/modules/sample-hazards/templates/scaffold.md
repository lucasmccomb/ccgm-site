# Scaffold Template

This file has `template: true` in module.json but contains no installer
placeholder text at all, so `hasSubstitutionPlaceholders` must be false for
this file even though template is true.
